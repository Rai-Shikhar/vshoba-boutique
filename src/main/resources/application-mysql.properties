# ============================================================
# MySQL profile - for real deployment.
# Activate it with:  mvn spring-boot:run -Dspring-boot.run.profiles=mysql
# ============================================================

spring.datasource.url=jdbc:mysql://localhost:3306/vshoba_boutique?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=change_me

# "update" is fine for now; switch to "validate" once tables are stable.
spring.jpa.hibernate.ddl-auto=update
